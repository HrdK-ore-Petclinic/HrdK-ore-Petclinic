name: CI/CD Pipeline — Prod

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Docker image tag to deploy (SHORT_SHA from dev pipeline)"
        required: true
      service:
        description: "Which service to deploy?"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - spring-petclinic-admin-server
          - spring-petclinic-api-gateway
          - spring-petclinic-config-server
          - spring-petclinic-customers-service
          - spring-petclinic-discovery-server
          - spring-petclinic-genai-service
          - spring-petclinic-vets-service
          - spring-petclinic-visits-service

jobs:

# ------------------------
# 1. BUILD & TEST
# ------------------------
  build-and-test:
    runs-on: [self-hosted, Linux, X64, k8s]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Run tests
        run: mvn test --batch-mode


# ------------------------
# 2. DEPLOY PROD (services)
# ------------------------
  deploy-prod:
    needs: build-and-test
    runs-on: [self-hosted, Linux, X64, k8s]
    environment: prod

    steps:
      - uses: actions/checkout@v4

      - name: Deploy all services to prod
        if: inputs.service == 'all'
        run: |
          IMAGE_TAG=${{ inputs.image_tag }}

          kubectl apply -f k8s/ -n prod

          for SVC in admin-server api-gateway config-server customers-service \
                      discovery-server genai-service vets-service visits-service; do
            kubectl set image deployment/${SVC} \
              ${SVC}=ross1832/spring-petclinic-${SVC}:${IMAGE_TAG} -n prod
          done

          kubectl rollout status deployment -n prod --timeout=300s

      - name: Deploy single service to prod
        if: inputs.service != 'all'
        run: |
          IMAGE_TAG=${{ inputs.image_tag }}
          SHORT=$(echo "${{ inputs.service }}" | sed 's/spring-petclinic-//')

          kubectl apply -f k8s/${SHORT}-deployment.yaml -n prod
          kubectl apply -f k8s/${SHORT}-service.yaml -n prod
          kubectl set image deployment/${SHORT} \
            ${SHORT}=ross1832/${{ inputs.service }}:${IMAGE_TAG} -n prod
          kubectl rollout status deployment/${SHORT} -n prod --timeout=300s


# ------------------------
# 3. DEPLOY PROD (monitoring)
# ------------------------
  deploy-prod-monitoring:
    needs: deploy-prod
    runs-on: [self-hosted, Linux, X64, k8s]
    environment: prod

    steps:
      - uses: actions/checkout@v4

      - name: Deploy monitoring stack
        run: kubectl apply -R -f k8s/monitoring/ -n monitoring-prod
