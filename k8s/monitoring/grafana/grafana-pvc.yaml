apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  namespace: monitoring-prod
spec:
  storageClassName: longhorn-prod   # added longhorn pvc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi